{
  "name": "medihub-central",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter medihub-central-web dev",
    "dev:mobile": "pnpm --filter hms-mobile start",
    "build:web": "pnpm --filter medihub-central-web build",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf node_modules && rm -rf node_modules"
  },
  "dependencies": {
    "@types/react-grid-layout": "^1.3.5",
    "react-grid-layout": "^1.5.1"
  }
}
